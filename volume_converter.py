def liters_to_milliliters(liters):
    """
    Converts the given liters to milliliters
    returns: the milliliters
    """
    conversion_factor = 1000
    milliliters = liters * conversion_factor

    return milliliters

